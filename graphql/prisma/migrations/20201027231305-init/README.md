# Migration `20201027231305-init`

This migration has been generated by ignatif at 10/28/2020, 1:13:05 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "email" TEXT NOT NULL
)

CREATE TABLE "Story" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "authorEmail" TEXT,
    "characterId" INTEGER,
    "entityId" INTEGER,

    FOREIGN KEY ("authorEmail") REFERENCES "User"("email") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("characterId") REFERENCES "Character"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("entityId") REFERENCES "Entity"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Chapter" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "authorEmail" TEXT,
    "storyId" INTEGER,

    FOREIGN KEY ("authorEmail") REFERENCES "User"("email") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("storyId") REFERENCES "Story"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Character" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "authorEmail" TEXT,

    FOREIGN KEY ("authorEmail") REFERENCES "User"("email") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Trait" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "key" TEXT NOT NULL,
    "value" TEXT NOT NULL,
    "characterId" INTEGER,
    "entityId" INTEGER,

    FOREIGN KEY ("characterId") REFERENCES "Character"("id") ON DELETE SET NULL ON UPDATE CASCADE,
    FOREIGN KEY ("entityId") REFERENCES "Entity"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Event" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "title" TEXT NOT NULL,
    "entityId" INTEGER,
    "chapterId" INTEGER NOT NULL,

    FOREIGN KEY ("chapterId") REFERENCES "Chapter"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("entityId") REFERENCES "Entity"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "Entity" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "slug" TEXT NOT NULL,
    "authorEmail" TEXT,

    FOREIGN KEY ("authorEmail") REFERENCES "User"("email") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "_CharacterToEvent" (
    "A" INTEGER NOT NULL,
    "B" INTEGER NOT NULL,

    FOREIGN KEY ("A") REFERENCES "Character"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("B") REFERENCES "Event"("id") ON DELETE CASCADE ON UPDATE CASCADE
)

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")

CREATE UNIQUE INDEX "Story.authorEmail_slug_unique" ON "Story"("authorEmail", "slug")

CREATE UNIQUE INDEX "Chapter.authorEmail_slug_unique" ON "Chapter"("authorEmail", "slug")

CREATE UNIQUE INDEX "Character.authorEmail_slug_unique" ON "Character"("authorEmail", "slug")

CREATE UNIQUE INDEX "Entity.authorEmail_slug_unique" ON "Entity"("authorEmail", "slug")

CREATE UNIQUE INDEX "_CharacterToEvent_AB_unique" ON "_CharacterToEvent"("A", "B")

CREATE INDEX "_CharacterToEvent_B_index" ON "_CharacterToEvent"("B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201027231305-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,78 @@
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+  experimentalFeatures = ["connectOrCreate"]
+}
+
+model User {
+  id             Int     @id @default(autoincrement())
+  email          String  @unique
+}
+
+model Story {
+  id             Int        @id @default(autoincrement())
+  title          String
+  slug           String
+  chapters       Chapter[]
+  author         User?      @relation("UserStoryAuthor", fields: [authorEmail], references: [email])
+  authorEmail    String?
+
+  @@unique([authorEmail, slug])
+}
+
+model Chapter {
+  id             Int     @id @default(autoincrement())
+  title          String
+  slug           String
+  events         Event[]
+  author         User?      @relation("UserChapterAuthor", fields: [authorEmail], references: [email])
+  authorEmail    String?
+
+  @@unique([authorEmail, slug])
+}
+
+model Character {
+  id             Int     @id @default(autoincrement())
+  name           String
+  slug           String
+  stories        Story[]
+  events         Event[]
+  traits         Trait[]
+  author         User?      @relation("UserCharacterAuthor", fields: [authorEmail], references: [email])
+  authorEmail    String?
+
+  @@unique([authorEmail, slug])
+}
+
+model Trait {
+  id             Int     @id @default(autoincrement())
+  key            String
+  value          String
+  character      Character?
+  entity         Entity?
+}
+
+model Event {
+  id              Int     @id @default(autoincrement())
+  title           String
+  characters      Character[]
+  chapter         Chapter
+}
+
+model Entity {
+  id             Int     @id @default(autoincrement())
+  name           String
+  slug           String
+  stories        Story[]
+  events         Event[]
+  traits         Trait[]
+  author         User?      @relation("UserEntityAuthor", fields: [authorEmail], references: [email])
+  authorEmail    String?
+
+  @@unique([authorEmail, slug])
+}
+
```


